export const state = () => ({})

export const mutations = {}

export const getters = {}

export const actions = {
  /* Auth */
  /* async register (state, { first_name, last_name, email, password }) {
    const registerResult = await this.$axios.post('/auth/register', {
      first_name, last_name, email, password,
    });
    console.log('registerResult',registerResult);
    if (!registerResult.data.access_token) {
      return registerResult
    } else {
      this.$auth.setUserToken(registerResult.data.access_token);
      return registerResult;
    }
  }, */
}
