# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: "Deploy testnets"
"on":
  push:
    branches:
      - dev
  workflow_dispatch:
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - uses: actions/checkout@v2
      - run: bash cli-dev.sh ci

      - name: "Building ropsten [prod]"
        run: yarn run ci:build:ropsten
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}"
          target: prod-ropsten
          projectId: zksync-vue
          channelID: live

      - name: "Re-building rinkeby [prod]"
        run: bash cli-dev.sh clean nuxt && yarn run ci:build:rinkeby
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}"
          target: prod-rinkeby
          projectId: zksync-vue
          channelID: live
