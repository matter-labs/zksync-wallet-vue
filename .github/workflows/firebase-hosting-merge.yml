# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - dev
      - rinkeby
  pull_request:
    branches:
      - dev
      - rinkeby
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: yarn ci

      - name: Configure for rinkeby
        if: github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/rinkeby'
        run: sh ./cli-process-env.sh rinkeby

      - name: Configure for ropsten
        if: github.ref == 'refs/heads/ropsten'
        run: sh ./cli-process-env.sh ropsten

      - name: Build
        run: yarn generate --fail-on-error

      # - name: Install firebase tools
      #   run: |
      #     sudo npm install -g firebase-tools
      #     firebase use zksync-vue --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}
      
      - name: Deploy to rinkeby site
        if: github.ref == 'refs/heads/rinkeby'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          # repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}'
          target: zksync-vue-rinkeby-2
          projectId: zksync-vue
        # env:
        #   FIREBASE_CLI_PREVIEWS: hostingchannels

      # - name: Deploy to staging site
      #   if: github.ref == 'refs/heads/dev'
      #   run: firebase deploy --only hosting:live --non-interactive --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }} -m "RunID ${{ github.run_id }} Commit SHA ${{ github.sha }}"

      # - name: Deploy to rinkeby site
      #   if: github.ref == 'refs/heads/rinkeby'
      #   run: firebase deploy --only hosting:zksync-vue-rinkeby-2 --non-interactive --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }} -m "RunID ${{ github.run_id }} Commit SHA ${{ github.sha }}"
