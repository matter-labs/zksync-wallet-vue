# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: "Deploy beta"
"on":
  push:
    branches:
      - beta
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: beta
    steps:
      - uses: actions/checkout@v2
      - run: bash cli-dev.sh ci

      - name: "[Rinkeby-beta release] Configuring rinkeby-beta env"
        run: yarn run ci:build:rinkeby-beta
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}"
          target: beta-rinkeby
          projectId: zksync-vue
          channelID: live

      - name: "Build ropsten-beta [beta]"
        run: yarn run ci:build:ropsten-beta
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZKSYNC_VUE }}"
          target: ropsten-beta
          projectId: zksync-vue
          channelID: live
